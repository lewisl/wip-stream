# Task Completion

- Run `npm run compile` after TypeScript changes.
- Run `npm run package` when manifest/contribution changes need packaging validation.
- Confirm `git status --short --branch` and review only task-related diff.